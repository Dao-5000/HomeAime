# 后端依赖（锁定版本，部署稳定）
# ★ 锁定为 venv 中实际验证通过的版本（2026-08-22）
#   ⚠ 不要回退到旧版：chromadb 1.x 与 0.5.x API 不兼容（memory/ 子包依赖 1.x）
fastapi==0.141.1
uvicorn[standard]==0.52.3
httpx==0.28.1
pycryptodome==3.23.0
pyinstaller==6.22.2
chromadb==1.5.9
faster-whisper==1.2.1
Pillow==10.4.0

# TTS（情绪语音）
edge-tts==7.2.8

# 语义记忆（向量召回）
sentence-transformers==6.0.0

# 上传/WS 运行时依赖
python-multipart==0.0.32
websockets==17.0.1
mss
